# Shared Package

Keep cross-app contracts here so backend and dashboard do not drift.

Expected early contents:

- `UsageLog` type
- `SummaryResponse` type
- model pricing tables
- calculation constants
