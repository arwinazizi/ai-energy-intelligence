{
  "name": "ai-energy-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Proxy-based measurement layer for AI usage.",
  "workspaces": [
    "apps/backend",
    "apps/dashboard",
    "packages/shared"
  ],
  "scripts": {
    "dev:backend": "npm.cmd --workspace @aei/backend run dev",
    "build:shared": "npm.cmd --workspace @aei/shared run build",
    "build:backend": "npm.cmd --workspace @aei/shared run build && npm.cmd --workspace @aei/backend run build",
    "dev:dashboard": "npm.cmd --workspace @aei/dashboard run dev",
    "build:dashboard": "npm.cmd --workspace @aei/dashboard run build",
    "preview:dashboard": "npm.cmd --workspace @aei/dashboard run preview"
  }
}
