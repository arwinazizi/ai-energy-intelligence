{
  "name": "ai-energy-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Proxy-based measurement layer for AI usage.",
  "workspaces": [
    "apps/backend",
    "apps/dashboard",
    "packages/shared"
  ],
  "scripts": {
    "dev:backend": "npm --workspace @aei/backend run dev",
    "build:backend": "npm --workspace @aei/backend run build",
    "dev:dashboard": "npm --workspace @aei/dashboard run dev",
    "build:dashboard": "npm --workspace @aei/dashboard run build",
    "preview:dashboard": "npm --workspace @aei/dashboard run preview"
  }
}
