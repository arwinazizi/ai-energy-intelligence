# Supabase

This directory holds database assets for the V1 prototype.

Start with:

- the minimal schema in `migrations/0001_v1_init.sql`

Keep V1 intentionally simple:

- no multi-tenant tables yet
- no dashboard auth tables yet
- no export pipeline yet
